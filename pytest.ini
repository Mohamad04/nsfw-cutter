[pytest]
pythonpath = .
testpaths = tests
markers =
    model_integration: downloads and runs the pinned Stage-1 ONNX model
    tinyclip_model_integration: downloads and runs the pinned TinyCLIP model
