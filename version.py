# This file is the single source of truth for the application version.
# On tagged releases the CI pipeline patches this line before running PyInstaller,
# replacing "dev" with the tag (e.g. "1.2.0").
__version__ = "dev"
